// 性能:全局变量累加循环(测试全局寄存器提升)
// sum(0..999) = 499500 → % 256 = 44
// expect: 44
int g = 0;

int main() {
    int i = 0;
    while (i < 1000) {
        g = g + i;
        i = i + 1;
    }
    return g % 256;
}
